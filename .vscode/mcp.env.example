# Optional values for advanced Supabase MCP setups (CI/manual auth)
# Do not commit real secrets.

# Optional: scope the MCP server to a single project
SUPABASE_PROJECT_REF=your_project_ref

# Optional: only for CI/manual-auth clients that require bearer token headers
SUPABASE_ACCESS_TOKEN=your_supabase_personal_access_token
